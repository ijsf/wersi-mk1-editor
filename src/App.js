import React, { Component } from 'react';
import WersiApp from 'components/WersiApp';

export default class App extends Component {
  render() {
    return (
      <WersiApp
        // Defines the version
        version="0.7.1"
      />
    );
  }
}
